#include <iostream>
using namespace std;
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

double a,b,c;

int main(int argc, char** argv) {

	cout << "Unesite tri realna broja: ";
	cin >> a >> b >> c;
	cout << "Aritmeticka sredina je: " << (a+b+c)/3;

	return 0;
}
